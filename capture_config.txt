# nr_pdsch_capture configuration
# Keys are whitespace-separated from values.
# output_file path is set at build time to <repo>/data/pdsch_dataset.bin.
# To override, add: output_file /your/custom/path.bin

max_captures  5000
